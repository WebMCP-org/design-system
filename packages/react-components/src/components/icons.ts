/**
 * Public leaf for the shared inline-SVG icon set, so sibling packages
 * (e.g. @mcp-b/think-chat) and app-level tool renderers can reuse the same
 * icons instead of duplicating SVGs.
 */
export * from "./_internal/icons";
